[alias]
	co = checkout
	st = status
	cm = commit
	br = branch
	bs = bisect
	sh = stash
	bD = branch -D
	rup = remote update -p
    rb = rebase
    rbc = rebase --continue
    rba = rebase --abort
    ap = add -p
	diffs = diff --staged
	bm = branch -m
	cob = checkout -b
	cma = commit --amend
	fall = fetch --all
	ba = branch -a
	b = branch
	stdp = stash drop
	stshp = stash show -p
	stsh = stash show
	dog = log --all --decorate --oneline --graph
	p = push
	pf = push --force
[user]
	email = <fill-here>
	name = Jean-Baptiste Lorteau
[core]
	editor = vim
	pager = diff-so-fancy | less --tabs=4 -RFX
